// This is a stub file to trick typescript into thinking that "pages-registry"
// module is available when it actually is provided via webpack "alias" field.
import { PagesRegistry } from '../types'

const pagesRegistry: PagesRegistry = {}

export default pagesRegistry
